# Build output and local Wrangler state.
.wrangler/
dist/

# SQL, which Prettier has no parser for. The migrations are hand-aligned and the
# comments in them carry the column conventions the schema depends on.
migrations/

# Wrangler owns this one.
#
# Prettier parses it happily, but wants to add trailing commas and expand the
# compact staging block into thirteen lines. Neither buys anything, and a deploy
# config is a bad place to take a formatting opinion — especially one that makes
# the file invalid as strict JSON, which is what a reader reaching for `jq` will
# assume it is.
wrangler.jsonc

# Generated by Wrangler from wrangler.jsonc; regenerated, never edited.
worker-configuration.d.ts
